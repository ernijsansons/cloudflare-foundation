/**
 * Scans extensions/ and generates registry for build-time registration.
 * No-op for template; extend when adding dynamic extensions.
 */
console.log("Extension codegen: no dynamic extensions.");
process.exit(0);
